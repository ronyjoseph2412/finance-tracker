interface IncomeType {
  type: string;
  description: string;
  amount: number;
  frequency: string;
  date: string;
  paidBy: string;
  bankName?: string;
}
export const getTransactionsSummary = (userFinancials: any) => {
    
  return {
  };
};
